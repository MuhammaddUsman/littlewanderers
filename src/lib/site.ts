export const SITE_URL = 'https://littlewanderers.netlify.app'
export const DEFAULT_OG_IMAGE = `${SITE_URL}/og-default.png`
// Used only as a fallback when a destination's own photo URL fails to load
export const BROKEN_IMAGE_FALLBACK = '/placeholder.png'
